/**
 * How customers reach the shop directly.
 *
 * Used-part buyers almost always want to see the exact piece before paying,
 * and the catalog carries placeholder images rather than photographs of each
 * unit. One number, defined once, so the storefront and the footer can never
 * drift apart.
 */
export const SHOP_PHONE_DISPLAY = "(430) 373-2043";

/** E.164 for `tel:` links, so a tap dials on mobile. */
export const SHOP_PHONE_TEL = "+14303732043";

/** The one-line ask that appears on every product page. */
export const PHOTO_REQUEST_LINE =
  "Want photos of this exact part? Call or text us and we'll send them over.";
